import React from 'react/addons';
let TestUtils = React.addons.TestUtils;
import TextHighlight from '../../index';
import { ok, equal } from 'assert';

/* eslint func-names:0 */
describe('react-text', function() {
  it('should have tests', function() {
    ok(false);
  });
});
